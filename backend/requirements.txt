fastapi==0.115.0
uvicorn[standard]==0.30.6
asyncpg==0.29.0
pydantic==2.8.2
pydantic-settings==2.4.0
pydantic[email]==2.8.2
PyJWT==2.9.0
slowapi==0.1.9
httpx==0.27.2
python-dotenv==1.0.1
python-multipart==0.0.12
alembic==1.13.1
sqlalchemy==2.0.36
# psycopg2 is the SYNC driver alembic uses for migrations (the runtime uses
# asyncpg). Without it, `alembic upgrade` fails with ModuleNotFoundError.
psycopg2-binary==2.9.9
beautifulsoup4==4.12.3
lxml==5.3.0
# Playwright required by fiscal_officer.py for Tax By Year flag enrichment.
# After pip install, run: playwright install chromium
playwright>=1.43.0
# msgpack required by shelby_mmlba.py to decode Airtable's binary XHR responses.
msgpack>=1.0.8
# openpyxl required by shelby_delinquent_tax.py to parse the Trustee's XLSX lawsuit list.
openpyxl>=3.1.0
curl_cffi==0.15.0
